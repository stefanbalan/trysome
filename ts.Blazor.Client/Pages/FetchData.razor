@page "/fetchdata"
@using Microsoft.AspNetCore.Components
@using ts.Domain
@using ts.Domain.Entities
@inject HttpClient Http

<h1>Weather forecast</h1>

<p>This component demonstrates fetching data from the server.</p>

@if (entities == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Date</th>
                <th>Temp. (C)</th>
                <th>Temp. (F)</th>
                <th>Summary</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var forecast in entities)
            {
                <tr>
                    <td>@forecast.Name</td>
                </tr>
            }
        </tbody>
    </table>
}

@functions {
    SampleEntity[] entities;

    protected override async Task OnInitAsync()
    {
        entities = await Http.GetJsonAsync<SampleEntity[]>("api/SampleData/WeatherForecasts");
    }
}
